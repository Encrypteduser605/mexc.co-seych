<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>MEXC Account Dashboard</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #121212;
            color: #fff;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background-color: #1a1a1a;
            padding: 20px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }

        .header img {
            height: 35px;
        }

        .header button {
            background-color: #f9b400;
            color: #1b1b1b;
            border: none;
            padding: 12px 30px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 4px;
        }

        .header button:hover {
            background-color: #e59d00;
        }

        .main-content {
            margin-top: 60px;
        }

        .login-signup-container {
            text-align: center;
        }

        .login-form, .signup-form {
            max-width: 400px;
            margin: 0 auto;
            background-color: #222;
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        .login-form input, .signup-form input {
            width: 100%;
            padding: 12px;
            margin: 12px 0;
            background-color: #333;
            border: 1px solid #555;
            color: white;
            border-radius: 4px;
            font-size: 14px;
        }

        .login-form button, .signup-form button {
            background-color: #f9b400;
            color: #1b1b1b;
            padding: 14px;
            width: 100%;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }

        .login-form button:hover, .signup-form button:hover {
            background-color: #e59d00;
        }

        .dashboard {
            margin-top: 60px;
            background-color: #222;
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .balance {
            font-size: 24px;
            margin-top: 20px;
            color: #f9b400;
        }

        .settings {
            margin-top: 30px;
        }

        .settings p {
            margin: 8px 0;
            font-size: 14px;
        }

        .settings button {
            background-color: #f9b400;
            color: #1b1b1b;
            border: none;
            padding: 12px 24px;
            border-radius: 4px;
            font-size: 14px;
            cursor: pointer;
        }

        .settings button:hover {
            background-color: #e59d00;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <img src="https://upload.wikimedia.org/wikipedia/commons/5/53/MEXC_Global_Logo.png" alt="MEXC Logo">
            <button class="button" onclick="showLogin()">Sign In</button>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <div class="login-signup-container">
                <!-- Login Form -->
                <div id="login-form" class="login-form">
                    <h3>Sign In to MEXC</h3>
                    <input type="text" id="phone" placeholder="Phone Number" value="+2482510123">
                    <input type="password" id="password" placeholder="Password" value="Tier$42@OwT&5">
                    <button onclick="login()">Login</button>
                    <p>Don't have an account? <span style="cursor: pointer;" onclick="showSignup()">Sign Up</span></p>
                </div>

                <!-- Signup Form -->
                <div id="signup-form" class="signup-form" style="display:none;">
                    <h3>Sign Up for MEXC</h3>
                    <input type="text" id="new-phone" placeholder="Phone Number">
                    <input type="password" id="new-password" placeholder="Password">
                    <button onclick="signup()">Sign Up</button>
                    <p>Already have an account? <span style="cursor: pointer;" onclick="showLogin()">Sign In</span></p>
                </div>

                <!-- Dashboard -->
                <div id="dashboard" class="dashboard" style="display:none;">
                    <div class="dashboard-header">
                        <h2>MEXC Dashboard</h2>
                        <button class="button" onclick="showSettings()">Settings</button>
                    </div>
                    <div class="balance">
                        <p><strong>Available Balance: </strong>$0.91</p>
                    </div>
                    <div class="settings" id="settings" style="display:none;">
                        <h4>Account Settings</h4>
                        <p><strong>Account Created:</strong> 2018</p>
                        <p><strong>Phone Number:</strong> +2482510123</p>
                        <button onclick="logout()">Logout</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function showLogin() {
            document.getElementById("login-form").style.display = "block";
            document.getElementById("signup-form").style.display = "none";
            document.getElementById("dashboard").style.display = "none";
        }

        function showSignup() {
            document.getElementById("login-form").style.display = "none";
            document.getElementById("signup-form").style.display = "block";
        }

        function login() {
            const phone = document.getElementById("phone").value;
            const password = document.getElementById("password").value;

            if (phone === "+2482510123" && password === "Tier$42@OwT&5") {
                document.getElementById("login-form").style.display = "none";
                document.getElementById("signup-form").style.display = "none";
                document.getElementById("dashboard").style.display = "block";
            } else {
                alert("Invalid login details.");
            }
        }

        function signup() {
            alert("Sign up functionality is not implemented in this demo.");
        }

        function showSettings() {
            document.getElementById("settings").style.display = "block";
        }

        function logout() {
            document.getElementById("settings").style.display = "none";
            document.getElementById("dashboard").style.display = "none";
            document.getElementById("login-form").style.display = "block";
        }
    </script>
</body>
</html>
